spring:
  application:
    name: todo

  datasource:
    url: jdbc:postgresql://localhost:5436/todo
    driver-class-name: org.postgresql.Driver
    username: acc_dev
    password: acc_dev12
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080


